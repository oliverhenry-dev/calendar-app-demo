Create a new React component for the calendar app.

Ask the user for:
1. Component name
2. What the component should do

Then:
1. Create the component file in `src/components/` with proper structure
2. Use functional components with hooks
3. Follow existing patterns in the codebase (check CalendarGrid.jsx, DayCell.jsx, etc.)
4. Add basic PropTypes or inline documentation
5. Export from the component file
6. Show how to import and use it